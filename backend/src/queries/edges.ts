/**
 * Data access for the edges table.
 *
 * Will hold: fetching every edge in a document to draw graph connections, bulk
 * inserting edges produced by the AI extraction step, and removing an edge when
 * a connection is rejected.
 */

export {}
