/**
 * Data access for the documents table.
 *
 * Will hold: listing a user's documents, fetching one by id, creating a
 * document on upload, advancing its status through the ingestion pipeline
 * (pending, processing, ready, failed), and deleting a document along with the
 * nodes, edges, and questions that cascade from it.
 */

export {}
